name: Create Release

on:
  push:
    branches:
      - main
    paths:
      - 'organizer.py'
  workflow_dispatch:

jobs:
  build:
    name: Build Executable
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Set PyInstaller data separator
        id: set_separator
        shell: bash
        run: |
          if [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            echo "separator=;" >> $GITHUB_OUTPUT
          else
            echo "separator=:" >> $GITHUB_OUTPUT
          fi

      - name: Build with PyInstaller
        run: >
          pyinstaller
          --name "File Organizer"
          ${{ matrix.os == 'macos-latest' && '--onedir' || '--onefile' }}
          --windowed
          --add-data "assets${{ steps.set_separator.outputs.separator }}assets"
          ${{ matrix.os == 'windows-latest' && '--icon "assets/app_icon.ico"' || '' }}
          ${{ matrix.os == 'macos-latest' && '--icon "assets/app_icon.icns"' || '' }}
          ${{ matrix.os == 'ubuntu-latest' && '--icon "assets/icon-1024x1024.png"' || '' }}
          organizer.py

      - name: Package Artifact (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          Compress-Archive -Path dist/"File Organizer.exe" -DestinationPath "File-Organizer-Windows.zip"
        shell: pwsh

      - name: Package Artifact (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          zip "File-Organizer-Linux.zip" dist/"File Organizer"

      - name: Package Artifact (macOS)
        if: matrix.os == 'macos-latest'
        run: |
          zip -r "File-Organizer-macOS.zip" "dist/File Organizer.app"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: File-Organizer-${{ matrix.os }}
          path: File-Organizer-*.zip

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
      
      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/*/*.zip
          draft: true
          generate_release_notes: true